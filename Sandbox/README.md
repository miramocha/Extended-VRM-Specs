# Sandbox (local experiments)

**Local only — not in git.** Throwaway notes, JSON dumps, and scratch files.

## Do not

- Link sandbox files from `specs/`, `decisions/`, or `implementations/`
- Commit sandbox files (gitignore blocks them except this README)

## Gitignore

| Path | In git? |
|------|---------|
| `Sandbox/*` | No |
| `Sandbox/README.md` | **Yes** |
